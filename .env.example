# Client-side (VITE_*) environment — baked into the browser bundle at build
# time. Copy to .env and fill in. NEVER commit .env.
VITE_APP_ENV=development
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY_DEV=your-dev-anon-key
VITE_SUPABASE_ANON_KEY_PROD=your-prod-anon-key
