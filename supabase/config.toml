project_id = "mxspjofeyklodydnergm"

# Edge functions have been migrated to TanStack Start server routes running on
# Cloudflare Workers (src/routes/api/*). This directory now only tracks the
# database migrations. See docs/CLOUDFLARE_DEPLOYMENT.md.
